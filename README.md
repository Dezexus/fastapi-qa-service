# FastAPI Q&A Service (Демонстрационный проект)

Сервис вопросов и ответов с REST API на FastAPI.

## 🚀 Технологии

- **FastAPI** - современный, быстрый веб-фреймворк для построения API
- **PostgreSQL** - реляционная база данных
- **Redis** - кэширование данных для повышения производительности
- **SQLAlchemy** - ORM для работы с базой данных
- **Alembic** - система миграций базы данных
- **Docker** - контейнеризация приложения
- **Pytest** - тестирование

## 📋 Функциональность

- Создание, получение и удаление вопросов
- Добавление ответов к вопросам
- **Кэширование вопросов в Redis** для ускорения ответов API
- Автоматическая инвалидация кэша при изменении данных
- Валидация данных с помощью Pydantic
- Обработка ошибок и кастомные исключения
- Логирование в формате JSON с ротацией файлов
- Автоматизированное тестирование
- Миграции базы данных через Alembic

## 🏃‍♂️ Быстрый старт

### Запуск с Docker Compose

1. Клонируйте репозиторий:
```bash
git clone https://github.com/Dezexus/fastapi-qa-service.git
cd fastapi-qa-service
```

2. Запустите приложение:
```bash
docker-compose up db web redis
```
2.1 Запуск с тестовой БД:
```bash
docker-compose up
```

3. Приложение будет доступно по адресу: http://localhost:8000

4. Документация API доступна по адресу: http://localhost:8000/docs

## 🎯 Особенности кэширования

- **Автоматическое кэширование** вопросов и списков вопросов
- **TTL кэша**: 1 час (настраивается)
- **Инвалидация кэша** при создании, обновлении или удалении данных
- **Отказоустойчивость** - при недоступности Redis приложение продолжает работу
- **Асинхронные операции** с Redis для максимальной производительности

## 📊 Мониторинг

- Логи приложения: `logs/qa_service.log`
- Логи ошибок: `logs/qa_service_errors.log`
- Health-check эндпоинт: `GET /health`

## 🧪 Тестирование

```bash
pytest -v
```

## 🔧 Конфигурация

Основные переменные окружения в файле `.env`:
- `DATABASE_URL` - подключение к основной базе данных
- `DATABASE_TEST_URL` - подключение к тестовой базе данных  
- `REDIS_URL` - подключение к Redis серверу

## 🏗️ Архитектура

```
app/
├── api/v1/          - Эндпоинты API
├── core/            - Конфигурация, исключения, логирование
├── crud/            - Операции с базой данных
├── models/          - SQLAlchemy модели
├── schemas/         - Pydantic схемы
└── services/        - Бизнес-логика (Redis кэширование)
```
